import { ColorBorderDateDirective } from './color-border-date.directive';

describe('ColorBorderDateDirective', () => {
  it('should create an instance', () => {
    const directive: ColorBorderDateDirective = new ColorBorderDateDirective();
    expect(directive).toBeTruthy();
  });
});
